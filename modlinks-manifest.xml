<!--
  Modlinks manifest for submission to https://github.com/hk-modding/modlinks
  (append to ModLinks.xml). Before submitting:
    1. Run scripts\package.ps1 to build the release zip and print its SHA256.
    2. Create a GitHub release, attach the zip, and put its download URL in Link.
    3. Replace REPLACE_WITH_SHA256 with the printed hash (uppercase hex).
  Publishing is a manual decision - this file is prepared, not submitted.
-->
<Manifest>
    <Name>Tool Assisted Steelsoul</Name>
    <Description>Insurance for Steel Soul runs: automatic save backups the moment the game writes them (outside the Steam Cloud folder), a ring of timestamped backups per slot, an in-game save manager to restore and reload a run without restarting, and an optional salvage-or-die prompt on Steel Soul death (off by default).</Description>
    <Version>1.0.0.0</Version>
    <Link SHA256="REPLACE_WITH_SHA256"><![CDATA[https://github.com/ASThome00/ToolAssistedSteelsoul/releases/download/v1.0.0.0/ToolAssistedSteelsoul.zip]]></Link>
    <Dependencies />
    <Repository><![CDATA[https://github.com/ASThome00/ToolAssistedSteelsoul]]></Repository>
    <Tags>
        <Tag>Utility</Tag>
    </Tags>
</Manifest>
